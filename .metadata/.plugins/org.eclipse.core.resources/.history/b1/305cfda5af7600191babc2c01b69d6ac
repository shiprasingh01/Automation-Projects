package pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class AlertPage {
	WebDriver driver;

	By alertTab = By.xpath("//nav[@class=\'block\']/div/ul/li[7]/a");
	//By simpleAlertBox = By.xpath("//*[@id='wrapper']/div/div[1]/div[1]/ul/li[1]/a");
	By alertBox = By.xpath("//html/body/button[contains(text(),\'alert box\')]");

	public AlertPage(WebDriver driver) {
		this.driver = driver;
	}

	public void clickAlertTab() {
		driver.findElement(alertTab).click();
		driver.switchTo().defaultContent();
		
	}

	public void handleAlert() {
		this.clickAlertTab();
		driver.switchTo().frame(1);
		//driver.findElement(simpleAlertBox).click();
		driver.findElement(alertBox).click();
	}
}
